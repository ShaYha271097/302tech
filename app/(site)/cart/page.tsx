import CartClient from "./CartClient";

export const metadata = {
  title: "Giỏ hàng",
  description: "Giỏ hàng của bạn tại 302 Tech",
};

export default function CartPage() {
  return <CartClient />;
}